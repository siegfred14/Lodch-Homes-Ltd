import React from "react";
import { FiMenu } from "react-icons/fi";

export const Navbar = () => {
  return (
    <nav>
      <p>Navbar</p>
      {/* Logo */}
      {/* User Sign in */}
      {/* Right Menu */}
      <FiMenu />
    </nav>
  );
};
